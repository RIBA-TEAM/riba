name: frontend
description: RIBA mobile application

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: "^3.10.0"

dependencies:

  provider: ^6.1.2

  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/